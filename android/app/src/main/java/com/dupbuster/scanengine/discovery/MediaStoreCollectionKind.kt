package com.dupbuster.scanengine.discovery

/** MediaStore volumes queried for Mode B platform discovery (requirements §5.2). */
enum class MediaStoreCollectionKind {
  IMAGE,
  VIDEO,
  AUDIO,
  DOWNLOAD,
}
