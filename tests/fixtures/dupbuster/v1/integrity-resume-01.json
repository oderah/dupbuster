{
  "id": "integrity-resume-01",
  "description": "CheckpointStore + relaunch ResumePromptBanner: resume from last_processed_id or start over (AC-integrity-resume-01)",
  "platform": "both",
  "input": {
    "scanRunStatus": "running",
    "lastProcessedId": 500
  },
  "expect": {
    "findResumableRun": true,
    "lastProcessedId": 500,
    "abandonClearsResumable": true,
    "resumePromptOffered": true
  }
}
